---
"@pstdio/pocketcoder-remote": minor
---

New package: a Pi-based terminal UI for PocketCoder workspaces. Runs local Pi as a thin client over the service relay with durable history replay, in-UI workspace picker/create/cancel commands, and a live workspace status bar.
