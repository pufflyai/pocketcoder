---
"@pstdio/pocketcoder-contracts": minor
"@pstdio/pocketcoder-client": minor
"@pstdio/pocketcoder-cli": minor
"@pstdio/pocketcoder-remote": minor
---

Add workspace-native file attachments: streamed uploads into `$HOME/.pcd/attachments`, attachment-aware AgentAPI messages, CLI `--file` and chat `/attach` flows, and Pi image, `@path`, and `/attach` input.
