{
	"name": "@pstdio/pocketcoder-auth",
	"version": "0.1.0",
	"private": true,
	"description": "Authentication and event-signing primitives for Pocketcoder.",
	"type": "module",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/pufflyai/pocketcoder.git",
		"directory": "packages/auth"
	},
	"files": [
		"src",
		"!src/**/*.test.ts"
	],
	"exports": {
		".": "./src/index.ts"
	},
	"engines": {
		"bun": ">=1.3.14"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --project ./tsconfig.json --noEmit"
	}
}
